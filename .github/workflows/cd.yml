name:  cd pipeline

on:
  workflow_run:
    workflows: ["ci pipeline"]
    types:
      - completed

jobs:
    build:
        
        runs-on: self-hosted

        steps:
            - name: pull docker image
              run: sudo docker pull ${{ secrets.DOCKER_USERNAME }}/testcicd-flower:latest
            - name: Delete old container
              run: sudo docker rm -f testcicd-flower-container || true
            - name: Run new container
              run: sudo docker run -d -p 3000:3000 --name testcicd-flower-container ${{ secrets.DOCKER_USERNAME }}/testcicd-flower